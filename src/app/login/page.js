import LoginForm from '../../components/auth/LoginForm';

export const metadata = {
  title: 'Login - Naagrik',
  description: 'Sign in to your Naagrik account',
};

export default function LoginPage() {
  return <LoginForm />;
}
