import SettingsPage from '../../components/pages/SettingsPage';

export const metadata = {
  title: 'Settings - Naagrik',
  description: 'Manage your account settings and preferences',
};

export default function Settings() {
  return <SettingsPage />;
}
