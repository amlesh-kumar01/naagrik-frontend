import StewardDashboardPage from '../../components/pages/StewardDashboardPage.jsx';

export const metadata = {
  title: 'Steward Dashboard - Naagrik',
  description: 'Community steward dashboard for managing local issues and responsibilities',
};

export default function StewardPage() {
  return <StewardDashboardPage />;
}
